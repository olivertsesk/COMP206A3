#include <stdio.h>
#include <stlib.h>

int main()
{
	char *filename;
	FILE *in,*out;
	printf("Enter the name of the file\n");

	scanf(filename);

	if(in!=null)
	{
		out=fopen(filename,w+)
	}

	fclose(fp);
	

}

